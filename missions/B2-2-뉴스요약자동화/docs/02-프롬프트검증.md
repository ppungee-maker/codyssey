# 요약 프롬프트 사전 검증

`00-실행가이드.md` STEP 4의 요약 프롬프트를 실제 기사로 미리 테스트한 기록.
`naeto chat`(내부적으로 `gemini-3-flash`)으로 검증 — Make에 넣을 프롬프트가 실제
Claude/OpenAI에서도 같은 구조로 동작할지 사전 확인하는 용도.

## 검증 대상 프롬프트

```
다음 뉴스 기사를 한국어로 3줄 이내로 요약해줘. 각 줄은 핵심 사실 하나씩만 담고,
불필요한 수식어는 빼줘.

제목: {title}
본문: {description}
```

## 샘플 기사 (실제 RSS 소스에서 가져옴)

- 출처: `https://techcrunch.com/feed/`
- 제목: *Anthropic launches Opus 5*
- 링크: https://techcrunch.com/2026/07/24/anthropic-launches-opus-5/
- 발행일: 2026-07-24
- 본문(발췌): Anthropic이 신규 모델 Opus 5를 출시. Fable 5보다 저렴·덜 제한적이며
  30일 데이터 보관 정책 미적용. 안전장치는 Fable 5 대비 85% 덜 작동할 것으로 예상.
  '자동 폴백' 베타 기능으로 안전 분류기 작동 시에도 저성능 모델의 응답을 받을 수 있음.

## 상태: ⏳ 검증 대기 (naeto chat 서버 오류)

2026-07-25 시도 시점에 `naeto chat`이 프리셋·모델 종류와 무관하게 전부
`event:error` / "일시적인 오류가 발생했습니다"를 반환(서버측 문제, 클라이언트
파싱 문제 아님 — SSE 원시 프레임으로 직접 확인함). 짧은 인사말("hi") 같은
최소 요청도 동일하게 실패해 내용 문제가 아니라 서비스 전반의 일시 장애로 판단.

`naeto image`(다른 프로바이더, OpenAI)는 같은 시점에 정상 동작 확인됨 — 챗/LLM
쪽만 영향받는 것으로 보임.

**서비스 복구되면 여기에 채울 것**:
1. 위 프롬프트로 실제 요약 결과 붙여넣기
2. 3줄 이내 조건 충족 여부 확인
3. 필요시 프롬프트 1차 수정 → 재검증 → 최종 확정
4. 사용된 vt 비용 기록
